body {
  min-height: 100vh;
  margin: 0;
  background-color: var(--dark-color);
  color: #f9f6ff;
  font-family: 'Georgia', serif;
  overflow: hidden;
}

.scene {
  min-height: 100vh;
  display: grid;
  place-items: center;
  position: relative;
  padding: 2rem;
}

.scene-content {
  position: relative;
  width: min(100vw, 1080px);
  max-width: 100%;
  min-height: 100vh;
  display: grid;
  place-items: center;
}

.night {
  position: fixed;
  inset: 0;
  z-index: 0;
  background: radial-gradient(circle at 50% 20%, rgba(255, 255, 255, 0.12), transparent 32%),
    radial-gradient(circle at 20% 80%, rgba(209, 169, 255, 0.14), transparent 24%),
    radial-gradient(circle at 85% 25%, rgba(203, 228, 255, 0.12), transparent 28%),
    linear-gradient(180deg, #110b1c 0%, #120f24 45%, #0b0816 100%);
  filter: blur(0.25vmin);
  pointer-events: none;
}

.gift-stage,
.flowers,
.message-container,
.audio-controls {
  position: relative;
  z-index: 2;
}

.hidden {
  opacity: 0;
  visibility: hidden;
  pointer-events: none;
}

.audio-controls {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 10;
  background: rgba(255, 255, 255, 0.06);
  border: 1px solid rgba(255, 255, 255, 0.12);
  border-radius: 14px;
  padding: 8px;
  backdrop-filter: blur(14px);
  transition: opacity 0.3s ease, transform 0.3s ease;
}

.audio-controls.show {
  opacity: 1;
  visibility: visible;
}

.play-button {
  appearance: none;
  border: none;
  background: rgba(255, 255, 255, 0.16);
  color: #f6efff;
  width: 44px;
  height: 44px;
  border-radius: 14px;
  cursor: pointer;
  font-size: 18px;
  display: grid;
  place-items: center;
  transition: transform 0.25s ease, background 0.25s ease;
}

.play-button:hover {
  transform: translateY(-1px) scale(1.03);
  background: rgba(255, 255, 255, 0.24);
}

.gift-stage {
  display: grid;
  place-items: center;
  min-height: 60vh;
}

.gift-box {
  position: relative;
  width: min(44vw, 360px);
  height: min(24vw, 180px);
  max-width: 360px;
  max-height: 180px;
  cursor: pointer;
  transform-style: preserve-3d;
  transform: translateZ(0);
  transition: transform 0.35s ease, opacity 0.35s ease;
  outline: none;
}

.gift-box:focus-visible {
  box-shadow: 0 0 0 4px rgba(255, 167, 68, 0.28);
}

.card-front,
.card-flap,
.card-stamp,
.card-lines,
.card-string,
.card-bow {
  position: absolute;
}

.card-front {
  width: 100%;
  height: 100%;
  bottom: 0;
  border-radius: 18px;
  background: linear-gradient(180deg, #d3b28b 0%, #b48c60 100%);
  box-shadow: 0 18px 40px rgba(0, 0, 0, 0.16);
  overflow: hidden;
  z-index: 2;
}

.card-front::before {
  content: '';
  position: absolute;
  inset: 0;
  background: linear-gradient(135deg, rgba(255,255,255,0.08) 0%, rgba(255,255,255,0) 75%);
  opacity: 0.24;
}

.card-front::after {
  content: '';
  position: absolute;
  left: 50%;
  top: 17%;
  width: 88%;
  height: 1px;
  transform: translateX(-50%);
  background: rgba(41, 29, 12, 0.08);
  filter: blur(0.5px);
}

.card-flap {
  width: 100%;
  height: 46%;
  top: 0;
  left: 0;
  clip-path: polygon(0 0, 100% 0, 50% 100%);
  background: linear-gradient(180deg, #f8ead1 0%, #e2c9a0 100%);
  box-shadow: 0 16px 28px rgba(0, 0, 0, 0.16);
  border-bottom: 1px solid rgba(0, 0, 0, 0.08);
  overflow: hidden;
  z-index: 4;
}

.card-flap::after {
  content: '';
  position: absolute;
  left: 12%;
  right: 12%;
  bottom: 6%;
  height: 12px;
  background: rgba(255, 255, 255, 0.18);
  border-radius: 999px;
  opacity: 0.4;
}

.card-stamp {
  width: 16%;
  height: 16%;
  bottom: 16%;
  left: 10%;
  background: linear-gradient(135deg, #f4b18c 0%, #d87f5f 100%);
  border-radius: 14px;
  border: 1px solid rgba(0, 0, 0, 0.08);
  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.3);
  z-index: 6;
}

.card-stamp::before {
  content: '';
  position: absolute;
  inset: 14%;
  border-radius: 12px;
  background: rgba(255, 255, 255, 0.25);
}

.card-stamp::after {
  content: '✿';
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  color: rgba(255, 255, 255, 0.88);
  font-size: 0.95rem;
}

.card-address {
  position: absolute;
  width: 32%;
  bottom: 16%;
  right: 10%;
  display: grid;
  gap: 0.45rem;
  z-index: 6;
}

.card-address span {
  display: block;
  height: 4px;
  border-radius: 999px;
  background: rgba(70, 44, 22, 0.34);
}

.card-address span:nth-child(2) {
  width: 80%;
}

.card-address span:nth-child(3) {
  width: 62%;
}

.card-lines {
  width: 30%;
  height: 4px;
  bottom: 12%;
  right: 10%;
  border-radius: 999px;
  background: rgba(33, 34, 34, 0.28);
  z-index: 5;
}

.card-lines::before,
.card-lines::after {
  content: '';
  position: absolute;
  right: 0;
  height: 4px;
  border-radius: 999px;
  background: rgba(33, 34, 34, 0.35);
}

.card-lines::before {
  width: 50%;
  top: -9px;
}

.card-lines::after {
  width: 65%;
  top: 10px;
}

.card-string {
  left: 50%;
  transform: translateX(-50%);
  background: transparent;
  z-index: 4;
}

.card-string--horizontal {
  top: 52%;
  width: 96%;
  height: 0;
  border-top: 2px solid rgba(24, 20, 17, 0.92);
}

.card-string--vertical {
  left: 50.5%;
  top: 8%;
  width: 0;
  height: 82%;
  border-left: 2px solid rgba(24, 20, 17, 0.92);
}

.card-bow {
  width: 26px;
  height: 26px;
  top: 53%;
  left: 51%;
  transform: translate(-50%, -50%);
  z-index: 5;
}

.card-bow::before,
.card-bow::after,
.card-bow span {
  content: '';
  position: absolute;
  background: rgba(40, 35, 28, 0.28);
  border-radius: 999px;
}

.card-bow::before {
  width: 12px;
  height: 18px;
  left: 1px;
  top: 5px;
  transform: rotate(-30deg);
}

.card-bow::after {
  width: 12px;
  height: 18px;
  right: 1px;
  top: 5px;
  transform: rotate(30deg);
}

.card-bow span {
  width: 8px;
  height: 12px;
  left: 50%;
  top: 7px;
  transform: translateX(-50%);
  border-radius: 999px;
}

.gift-box.opened .card-flap {
  transform: translateX(-50%) translateY(-12%) rotateX(-88deg);
}

.gift-label {
  position: absolute;
  left: 50%;
  bottom: -2.5rem;
  transform: translateX(-50%);
  color: rgba(255, 255, 255, 0.92);
  font-size: 1rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  text-shadow: 0 10px 24px rgba(0, 0, 0, 0.18);
}

.gift-box.breathing {
  animation: gift-breath 4s ease-in-out infinite;
}

.gift-box.opening {
  animation: gift-press 0.6s ease forwards;
}

.gift-box.opened .card-flap {
  transform: translateX(-50%) translateY(-15%) rotateX(-80deg);
}

.gift-box.opened .card-front,
.gift-box.opened .card-flap,
.gift-box.opened .card-seal,
.gift-box.opened .card-lines {
  transition: opacity 0.45s ease, transform 0.45s ease;
}

.gift-box.opened .gift-label {
  opacity: 0;
}

.gift-box.hidden-gift {
  opacity: 0;
  visibility: hidden;
  transform: translateY(-28px) scale(0.94);
  pointer-events: none;
  z-index: -1;
}

.gift-box.hidden-gift .card-front,
.gift-box.hidden-gift .card-flap,
.gift-box.hidden-gift .card-seal,
.gift-box.hidden-gift .card-lines,
.gift-box.hidden-gift .gift-label {
  opacity: 0;
}

@keyframes gift-breath {
  0%, 100% { transform: scale(1); }
  50% { transform: scale(1.02); }
}

@keyframes gift-press {
  0% { transform: translateY(0); }
  50% { transform: translateY(-12px); }
  100% { transform: translateY(0); }
}

@media (max-width: 640px) {
  .gift-box {
    width: min(70vw, 240px);
    height: min(54vw, 190px);
  }

  .gift-label {
    font-size: 0.95rem;
  }
}
