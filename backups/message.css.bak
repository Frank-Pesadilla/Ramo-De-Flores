.message-container {
  position: fixed;
  bottom: 5vh;
  left: 50%;
  transform: translateX(-50%);
  max-width: 80vw;
  z-index: 10;
  opacity: 0;
  visibility: hidden;
  transition: opacity 0.9s ease, visibility 0.4s ease;
}

.message-container.visible {
  opacity: 1;
  visibility: visible;
}

.message-box {
  background: rgba(255, 250, 248, 0.12);
  border: 1px solid rgba(255, 255, 255, 0.14);
  border-radius: 28px;
  padding: 28px 32px;
  box-shadow: 0 24px 80px rgba(9, 7, 20, 0.3);
  backdrop-filter: blur(20px);
}

.message-text {
  color: #fffdf9;
  font-size: clamp(1rem, 1.1vw, 1.2rem);
  line-height: 1.5;
  text-align: center;
  letter-spacing: 0.02em;
}

.paragraph {
  transition: opacity 0.4s ease;
  margin: 0 0 1rem;
  opacity: 0;
  min-height: 1.3rem;
}

.paragraph:last-child {
  margin-bottom: 0;
}

.highlight {
  color: #d8b1ff;
  font-weight: 600;
}

.message-box::before {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  box-shadow: inset 0 0 48px rgba(255, 255, 255, 0.08);
  pointer-events: none;
}
